import QenTerraComponents

let rowState = InteractiveRowState(isLoading: true)
let buttonState = DesignButtonState(isLoading: true)
print(DesignButtonRole.primary.rawValue)
print(rowState.isLoading)
print(buttonState.isLoading)
