# Знаки и баннеры QenTerra

## Каноническая палитра

Текущий векторный мастер использует два фиксированных брендовых нейтрала:

- средний графит `#6A6A73`;
- мягкий белый `#E8E8EC`.

Это цвета готовых знаков, а не замена семантических UI-токенов. Нельзя брать цвет пипеткой из растра, приблизительно воссоздавать его или глобально заменять внутри утверждённого экспорта.

## Семейства ассетов

### Логотипы

- `logos/vector/QenTerra Logo.svg` — единственный масштабируемый мастер; он предпочтителен для тёмных графитовых поверхностей, web, печати и производных экспортов.
- `logos/raster/` содержит прозрачные версии 512, 1024 и 2048 пикселей, отрендеренные из этого мастера.
- Поставленные обработки `Dry` и `Wet` в 1024 пикселя — отдельные утверждённые растры; у каждой есть версия `on Graphite` с непрозрачным контрастным полем.
- Нельзя придумывать варианты `Filled`, `Outlined`, `Dark` или `Light`. Файл выбирают по фактическому контрасту поверхности и требованию delivery.

### Баннеры

- `banners/vector/` предпочтителен для адаптивных шапок, документации и публикационных материалов.
- `banners/raster/` содержит версии 1024, 2048 и 4096 пикселей.
- Стандартный баннер — основная семейная подпись.
- Alternative — намеренный вариант, а не автоматическая замена основной формы.

### Платформенные публикационные материалы

- `platforms/youtube/YouTube Channel Banner (2560x1440px).png` — фиксированный экспорт шапки YouTube-канала; сохранять нативный холст и safe area.
- `platforms/youtube/YouTube Watermark (150px).png` — поставленный watermark для видео, а не замена основного логотипа.
- `platforms/buy-me-a-coffee/Buy Me a Coffee Banner (1600x400px).png` — поставленный баннер благодарности для поддержки.
- Эти платформенные экспорты не заменяют адаптивные семейства баннеров и логотипов; их нельзя обрезать, перекрашивать или переиспользовать для другого канала.

## Размещение

- Сохранять пропорции и внутреннюю геометрию.
- Оставлять вокруг полного знака свободную зону; если отдельной спецификации нет, практический минимум — высота наименьшего основного символа.
- Держать знак целиком внутри safe area; не обрезать надпись, контур или окончания ради подгонки layout.
- Использовать один знак в одной визуальной зоне. Повтор логотипа не усиливает бренд — он просто повышает плотность логотипов, достижение сомнительное.
- На фото или иллюстрации создавать спокойное контрастное поле, а не добавлять случайные тени, glow и дополнительные обводки.

## Запрещённые трансформации

- перекраска, градиенты, прозрачные fade, текстуры и blend modes;
- поворот, skew, растяжение, перспектива и раздельное движение частей;
- пересборка живым шрифтом или подмена глифов;
- дополнительная рамка, контейнер, badge или слоган без документированной композиции;
- смешивание или пересборка частей из отдельных экспортов;
- малый растр при наличии вектора или большего канонического файла.

## Выбор экспорта

Использовать SVG, когда потребитель безопасно его поддерживает. PNG нужен фиксированным растровым пайплайнам, скриншотам, магазинам и платформам с таким требованием. Растр должен отображаться не крупнее своего нативного размера; маленький файл нельзя апскейлить и объявлять новым мастером.

Каждый выдаваемый файл должен присутствовать в [`../../assets/brand/manifest.json`](../../assets/brand/manifest.json). Производный экспорт становится каноническим только после именования, размещения, добавления в манифест, проверки на светлом/тёмном фоне и документирования новой роли.

## Миграция с 2.x

Пути `Dark/Light Logo` и `Filled/Outlined` удалены в 3.0. Масштабируемые потребители переходят на `logos/vector/QenTerra Logo.svg`. Прозрачный растр используют только не крупнее нативного размера; когда знаку нужно собственное непрозрачное контрастное поле на светлой или насыщенной поверхности, берут утверждённый растр `on Graphite`.

## Контактные каналы

- [`contact@qenterra.com`](mailto:contact@qenterra.com) — общий адрес компании и бренда для вопросов, партнёрств, прессы, деловой и юридической переписки.
- [`support@qenterra.com`](mailto:support@qenterra.com) — адрес продуктовой поддержки для помощи, диагностики проблем, сообщений об ошибках и вопросов доступности.

Ни один из адресов не заменяет приватный канал сообщения об уязвимостях конкретного репозитория. Точные машиночитаемые роли находятся в [`../../registry/contact-channels.json`](../../registry/contact-channels.json); интерфейс и документация не должны менять их местами или придумывать новые назначения.
